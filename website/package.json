{
  "name": "chasecappo",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "pages:dev": "wrangler pages dev dist/",
    "deploy": "astro build && wrangler pages deploy dist/ --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^5.7.10",
    "@astrojs/sitemap": "^3.3.1"
  },
  "devDependencies": {
    "wrangler": "^4.14.0",
    "typescript": "^5.8.3"
  }
}
